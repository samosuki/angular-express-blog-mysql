h2 Edit post
label(for='title') Title:
form(name='myForm') 
input(ng-model='title', name='title')
p Body:
textarea(ng-model='editForm', cols='50', rows='15')
p
  button(ng-click='editPost()') Edit Post
p status: {{status}}

div(ng-repeat='posts in posts')
  h3 {{posts.title}}
  div {{posts.posts}}